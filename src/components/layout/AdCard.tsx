import Reveal from "../animations/reveal";

const AdCard = () => {
  return (
    <Reveal>
      <div className="" data-reveal>
        Advertisement Card here
      </div>
    </Reveal>
  );
};

export default AdCard;
